 Your Goal: Describe the Number

1- The function checkNumber takes a single argument: a number num
2- The function should return the string positive if the number is positive, negative if the number is negative, and zero if the number is zero.


------

answer


function checkNumber(num) {
    let result = ""
    if(num > 0){
        result += "positive"
    }
    else if (num < 0){
        result += "negative"
    }
    else{
        result += "zero"
    }
    return result
    
}

module.exports = checkNumber;
