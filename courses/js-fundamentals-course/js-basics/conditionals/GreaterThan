Your Goal: Which is Greater?

1-Given two arguments, first and last, determine which one is greater.
2- If first is greater than last, return first!
3- If last is greater than first, return last!
4- If they are equal, do not return anythi


------


answer


function greater(first, last) {
    if(first===last){
        return undefined
    }

    if(first>last){
        return first
    }
    else{
        return last
    }
    
}

module.exports = greater;
