Your Goal: Complete the Factorial

1- Taking in some integer value n, find the factorial for that number and return it.



-------


answer


function factorial(n) {
    let result = 1;
    for (let i = 1; i <= n; i++) {
        result *= i;
    }
    return result;
}

module.exports = factorial;
